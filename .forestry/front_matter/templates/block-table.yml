---
label: 'Block: Table'
hide_body: true
fields:
- name: header_columns
  type: field_group_list
  fields:
  - name: columns
    type: text
    config:
      required: false
    label: Columns
  config:
    min: 
    max: 
    labelField: 
  label: Header columns
