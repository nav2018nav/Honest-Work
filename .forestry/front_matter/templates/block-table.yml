---
label: 'Block: Table'
hide_body: true
fields:
- name: header_row
  type: field_group_list
  fields:
  - name: columns
    type: text
    config:
      required: false
    label: Columns
  config:
    min: 
    max: 
    labelField: 
  label: Header row
